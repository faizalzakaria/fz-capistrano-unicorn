RSpec.describe Fz::Capistrano::Unicorn do
  it "has a version number" do
    expect(Fz::Capistrano::Unicorn::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
